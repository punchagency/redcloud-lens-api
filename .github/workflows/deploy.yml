name: Deploy to Staging

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - 
        name: Checkout code
        uses: actions/checkout@v2
      - 
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - 
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - 
        name: Build and push Docker image
        run: |
          docker build -t nlq-api:v1.0.0 .
          docker tag nlq-api:v1.0.0 ${{ secrets.DOCKER_USERNAME }}/nlq-api:v1.0.0
          docker push ${{ secrets.DOCKER_USERNAME }}/nlq-api:v1.0.0
      # - 
      #   name: Retrieve SSH key
      #   run: |
      #     mkdir -p ~/.ssh
      #     echo "${{ secrets.SSH_PRIVATE_KEY }}" | base64 --decode > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
      # - 
      #   name: Copy files to DigitalOcean
      #   run: |
      #     scp  -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa -r ./docker-compose.yml ./nginx ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:~/app
      # - 
      #   name: Deploy with appleboy
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{secrets.SSH_KEY}}
      #     script: |
      #       cd ~/app
      #       export $(cat .env|xargs)
      #       docker login -u ${{ secrets.DOCKER_USERNAME}} -p ${{ secrets.DOCKER_PASSWORD }}
      #       docker pull ${{ secrets.DOCKER_USERNAME }}/nlq-api:v1.0.0
      #       docker compose up -d
      # - 
      #   name: Notify deployment status
      #   run: echo "Deployment to staging server complete"
